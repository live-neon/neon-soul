# OpenClaw + NEON-SOUL Environment Configuration
#
# Copy this file to .env and configure:
#   cp .env.example .env
#
# Required for OpenClaw:
#   - ANTHROPIC_API_KEY (or OPENAI_API_KEY)
#
# Optional:
#   - OPENCLAW_WORKSPACE: Custom workspace path (default: ~/.openclaw/workspace)
#   - OPENCLAW_LOG_LEVEL: Logging verbosity (default: info)

# =============================================================================
# REQUIRED: At least one LLM API key
# =============================================================================

# Anthropic Claude API key (primary for OpenClaw)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API key (optional, for GPT models)
OPENAI_API_KEY=

# =============================================================================
# OPTIONAL: OpenClaw Configuration
# =============================================================================

# Workspace directory path
# Default: ~/.openclaw/workspace
# Contains: AGENTS.md, SOUL.md, USER.md, IDENTITY.md, TOOLS.md, memory/
OPENCLAW_WORKSPACE=~/.openclaw/workspace

# Log level: debug, info, warn, error
OPENCLAW_LOG_LEVEL=info

# =============================================================================
# NEON-SOUL Configuration (for extraction service)
# =============================================================================

# Memory path inside container (usually don't change)
OPENCLAW_MEMORY_PATH=/memory

# Node environment
NODE_ENV=development

# =============================================================================
# Security Notes
# =============================================================================
#
# - NEVER commit this file with real API keys
# - Memory files may contain PII - handle with care
# - Consider using Docker secrets for production
# - Review OpenClaw's data retention policies
